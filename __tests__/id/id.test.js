import { render } from "react-dom";

describe("Given an [id] page", () => {
  describe("When receives a tuit id", () => {
    test("Then it should render a heading with the text `Tuitah`", () => {
      const header = "Tuitah";
      render(<>[id]</>);
    });
  });
});
